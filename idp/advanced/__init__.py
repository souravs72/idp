# Copyright (c) 2026, Sanjay Kumar and contributors
# For license information, please see license.txt

"""Phase 15 — Advanced Features.

Optional capabilities that extend the core pipeline:

* ``templates``      — user-defined extraction templates + matcher.
* ``batch``          — multi-file / ZIP batch processing.
* ``feedback``       — correction collection + few-shot prompt builder.
* ``tables``         — advanced table handlers (multi-page / borderless / nested).
* ``workflow``       — ERPNext workflow routing + notifications.
* ``prompt_library`` — industry prompt gallery.
* ``fine_tuning``    — training-dataset export from correction log.

Everything here is opt-in and feature-flagged via IDP Settings.
"""
